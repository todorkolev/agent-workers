{
  "mcpServers": {
    "agent-workers": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/agent-workers.mjs"],
      "env": {
        "AGENT_WORKERS_SUPERVISOR": "${CLAUDE_PLUGIN_ROOT}/dist/supervisor.mjs",
        "AGENT_WORKERS_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
